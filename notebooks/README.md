Notebooks in this folder are for exploration and reporting only -- they
should import and call functions from `factor_project`, not redefine logic
inline. Planned notebooks (created once the corresponding src modules are
implemented):

- `01_data_exploration.ipynb` -- validate universe pull, factor data,
  correlation heatmaps (mirrors the EDA already done in the original
  factor_models.ipynb).
- `02_factor_regressions.ipynb` -- per-stock and portfolio FF3/FF5
  regressions, full diagnostic suite, optional Fama-MacBeth appendix.
- `03_portfolio_backtest.ipynb` -- runs/visualizes the walk-forward
  backtest results and method comparison table.
